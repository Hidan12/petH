<!DOCTYPE html>
<html lang="en">
    <%- include('../partial/head')%>
<body class="errorviews">
    <!-- header -->
    <%- include('../partial/header')%>
    <article class="error">
        <p><%= box %></p>
        <% if (locals.img) { %>
            <picture>
                <img src="<%= "/img/error/" + img %> " alt="">
            </picture>
        <% } %>
    </article>

    

    <!-- footer -->
    <%- include('../partial/footer')%>
</body>

</html>